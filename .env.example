# Shared
PODIUM_SERVER_URL=http://localhost:3000     # Reed's tunnel URL when live
ANTHROPIC_API_KEY=                          # Haiku vision scoring, Sonnet cues + recap

# Adam (audio/)
DEEPGRAM_API_KEY=
OPENAI_API_KEY=                             # fallback transcription only

# Optional
YOODLI_API_KEY=                             # recap enrichment only, see docs/YOODLI.md
OPENROUTER_API_KEY=                         # OpenAI transcription models via OpenRouter
PODIUM_TRANSCRIBER=deepgram                 # deepgram | openrouter
